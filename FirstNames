function formatName(user) {
  return user.firstName + '!';
}
const user = {
  firstName: 'leyanna'
}
const element = <h1>bannanas {formatName(user)} +! </h1>;

ReactDOM.render(element, element, element, document.getElementById('root'));
